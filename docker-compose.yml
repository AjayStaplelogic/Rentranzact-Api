version: '3'
services:
 mongo_db:
    container_name: db_container
    image: mongo:latest
    restart: always
    volumes:
      - mongo_data:/data/db
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:3001"
    volumes:
      - /usr/src/app
      - /usr/src/app/node_modules
