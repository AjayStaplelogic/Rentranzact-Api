const PropertiesCategories = {
  RESIDENTIAL: "residential",
  COMMERCIAL: "commercial",
  SHORT_STAY: "short stay",
};

const PropertiesStatus = {
  RENTED: "rented",
  AVAILABLE: "available"
};

const RentType = {
  MONTHLY: "monthly",
  YEARLY: "yearly",
  QUATERLY: "quaterly"
}

const Type = {
  DETACHED_DUPLEX: "detached duplexes"
}

const FurnishingType = {
  FULLY_FURNISHED: "fully furnished"
}

const RentBreakDownPer = {
  AGENCY_FEE: 10,
  AGENT_FEE_PERCENT: 7,
  RTZ_FEE_PERCENT: 5,
  LEGAL_FEE_PERCENT: 10,
  CAUTION_FEE_PERCENT: 10,
}

const ApprovalStatus = {
  PENDING: "pending",
  APPROVED: "approved",
  REJECTED: "rejected"
}

const EAccountType = {
  local: "local",
  dom: "dom"
}


export {
  PropertiesCategories,
  PropertiesStatus,
  RentType,
  Type,
  FurnishingType,
  RentBreakDownPer,
  ApprovalStatus,
  EAccountType
};
